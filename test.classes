class Series
  public Series();
  public virtual ~Series();
  public virtual Real compute(UInt N) = 0;
  public pure virtual Real computeItem(UInt N);

class AlgebraicSeries(Series)
      public void compute(int arg1, double arg2)